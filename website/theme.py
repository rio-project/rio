import reflex as rx

THEME = rx.Theme(
    # primary_color=rx.Color.from_rgb(0.9, 0.7, 0),
)


# On desktop, most of the website is located in a centered column. This is the
# width of that column.
CENTER_COLUMN_WIDTH = 75

# Large corner radius used for style, when space is available
LARGE_CORNER_RADIUS = 3
